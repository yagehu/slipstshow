{
  "name": "slipstshow",
  "license": "GPL-3.0-or-later",
  "author": "",
  "type": "module",
  "private": true,
  "module": "lib.ts",
  "scripts": {
    "build": "esbuild lib.ts --bundle --outfile=build/slipstshow.js && esbuild src/slipstshow.css --bundle --outdir=build"
  },
  "dependencies": {
    "@preact/signals-core": "^1.13.0",
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "esbuild": "0.27.3"
  }
}
